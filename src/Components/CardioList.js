import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const CardioList = ({item, index}) => {
  return console.log(item, index);
};

export default CardioList;

const styles = StyleSheet.create({});
